const repoName = 'Boxed-Inventory-App';

function describeSolidRefactorNote() {
  return `${repoName}: prefer small modules with one clear responsibility.`;
}

module.exports = { describeSolidRefactorNote };
